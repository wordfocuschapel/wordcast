<h1>Word Focus Chapel Word Casts</h1>
<div>
  <ul>
  <% @media.each do |medium| %>
    <li><%= link_to medium.title, medium %>
    </li>
  <% end %>
  </ul>
</div>

<% content_for :head, auto_discovery_link_tag(:atom, media_url(format: "atom")) %>

<%= link_to "rss", media_url(format: "atom") %>